(function(window, $) {
  var Game = Game || {};
  Game.init = function (canvas) {
    Game.canvas = $(canvas);
  };

  Game.play = function () {
  };

  Game.event_loop = function () {
  };

})(window, $);
